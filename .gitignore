# Source assets (large originals kept locally, not published)
*.png
*.jpg
*.jpeg

# ...but publish this one supporting image intentionally
!hero-uncropped-eye.png

# Supporting mock-up files — not linked from index.html, but published
# so they're available if needed:
#   Email to John Ternus.html
#   open-ipax-pitch-deck-thumbnails-mockup.html
#   open-ipax-site-mockup.html
